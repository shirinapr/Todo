import { Button } from 'antd';
import React from 'react';

const Tasks = () => {
  return (
    <div>
      <Button type="primary" shape="circle">
        +
      </Button>
    </div>
  );
};

export default Tasks;
